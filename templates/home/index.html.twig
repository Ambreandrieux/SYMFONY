{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <a href="{{ path('app_brand_form') }}">New brand</a>
    <ul>
    {% for brand in brands %}
        <li>
            <a href="{{ path('app_brand_form_edit', {brand: brand.id}) }}">
                {{ brand.name }}
            </a>
        </li>
    {% endfor %}
    </ul>
{% endblock %}
