{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.category) }}
        <button type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}